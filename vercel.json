{
    "build": {
      "env": {
        "NODE_ENV": "production"
      },
      "commands": {
        "preinstall": "apt-get update && apt-get install -y libcairo2-dev libjpeg-dev libpango1.0-dev libgif-dev librsvg2-dev build-essential g++",
        "install": "npm install"
      }
    }
  }